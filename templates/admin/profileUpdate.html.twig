{% extends 'admin.html.twig' %}

{% block text %}
    <div class="container mt-3 mb-3">
        {{ form(form) }}

        <a href="{{ path('admin_profile') }}" class="text-decoration-none text-black link">Terug naar mijn profiel</a>
    </div>

    <style>
        .link:hover{
            color: #38723c;
            background-color: #6fc278;
        }
    </style>
{% endblock %}