{% extends 'base.html.twig' %}

{% block body %}
    {% block navbar %}
        <nav class="navbar navbar-expand-lg">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('member_home') }}"><img src="{{ asset('img/logo.jpg') }}" width="30%"></a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"><i class="bi bi-border-width"></i></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('member_home') }}">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('member_lesson') }}">lesoverzicht</a>
                        </li>
                        <li class="nav-item">
                        </li>
                    </ul>
                    <ul class="navbar-nav">
                        <li class="nav-item text-end">
                            <a class="nav-link" href="{{ path('member_profile') }}">Mijn profiel</a>
                        </li>
                        <li class="nav-item text-end">
                            <a class="nav-link" href="{{ path('logout') }}">Uitloggen</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
        <img src="{{ asset('img/banner.jpg') }}" width="100%">
    {% endblock %}

    {% block text %} {% endblock %}


    {% block footer %}
        <footer class="m-0">
            <div class="container-fluid m-0 p-0">
                <div class="card text-center footer">
                    <div class="card-header text-white">
                        <h6>&copy Trainings Factory Den Haag</h6>
                    </div>
                </div>
            </div>
        </footer>
    {% endblock %}

{% endblock %}
